# utils/sentiment_sources.py

import random

def get_google_trends_score(token):
    # Fake mock: replace with real Google Trends logic
    return round(random.uniform(0.1, 1.0), 2)

def get_twitter_sentiment_score(token):
    # Fake mock: replace with Twitter NLP sentiment later
    return round(random.uniform(-1.0, 1.0), 2)
